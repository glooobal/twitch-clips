// Clip-related constants
export const CLIP_CONFIG = {
    TIME_THRESHOLD: 10 * 60 * 1000, // Time threshold between clips (10 minutes)
    MIN_TOTAL_DURATION: 7 * 60 * 1000, // Minimum total duration of clips (7 minutes)
    MAX_CLIPS_PER_STREAMER: 2, // Maximum number of clips per streamer
    MIN_VIEWS: 125, // Minimum number of views required for a clip
    MIN_WOMEN_CLIPS: 3 // Minimum number of clips from women streamers
};

// Streamer-related constants
export const STREAMER_CONFIG = {
    MIN_VIEWERS: 50
}; 